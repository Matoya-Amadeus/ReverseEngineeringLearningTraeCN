# Phase 2 Core Domain Status

Date: 2026-03-18

## Implemented
- Workspace manager: `src/core/workspace-manager.mjs`
  - recent workspace index (dedupe + cap)
  - multi-root workspace model (`kind=multi-root`, `roots[]`)
  - restore/snapshot support
- Session manager: `src/core/session-manager.mjs`
  - owner lease locking for multi-window/process arbitration
  - conflict session state + forced takeover support
  - heartbeat/ownership release/stale recovery
  - restore/snapshot support
- Settings manager: `src/core/settings-manager.mjs`
  - layered settings chain: default/user/remote/workspace
  - remote versioned merge (`version-first`/`lww`)
  - conflict log capture and restore/snapshot support
- Core persistence store: `src/core/core-state-store.mjs`
  - schema: workspace + session(owner/session) + settings(meta/conflicts)
  - backward compatibility with old session shape
- Project bootstrap wiring: `src/core/project-bootstrap.mjs`
  - initialize/restore/persist/openWorkspace/endSession
  - dead-owner detection and auto-recovery at startup
  - heartbeat/takeover APIs
  - remote settings merge API
  - extension host/task runtime startup hooks
- Extension host runtime: `src/core/extension-host-manager.mjs`
  - isolated subprocess host (`extension-host-worker`)
  - start/restart/status lifecycle
  - crash injection + auto-restart policy (window/max/restart delay)
  - workspace/session binding + restart history
- Task runtime: `src/core/task-runtime-manager.mjs`
  - task catalog/list/run/history
  - real subprocess executor with timeout and stdout/stderr capture
  - workspace binding with safe cwd fallback
- Shell bridge channels integrated:
  - `workspace:open`
  - `workspace:recent`
  - `session:get`
  - `session:ownership`
  - `session:heartbeat`
  - `session:takeover`
  - `settings:get`
  - `settings:set`
  - `settings:merge-remote`
  - `settings:conflicts`
  - `ext:status`
  - `ext:restart`
  - `ext:crash`
  - `task:list`
  - `task:run`
  - `task:history`

## Validation
- Core contract: `tests/core/core-contract.mjs`
- Core suite: `tests/core/run-core-tests.mjs`
- Shell contract includes arbitration + merge + extension/task checks: `tests/shell/shell-contract.mjs`
- Included in global gate: `tests/auth/run-all-checks.mjs`
- Launcher validation: `launcher/open-reconstructed.command`

## Remaining to reach near-original core parity
1. Session arbitration across truly concurrent live GUI windows (not only local lease model).
2. Remote settings server reconciliation with authoritative revision ids + ack pipeline.
3. Extension host IPC/API parity with real extension contribution points.
4. Task runtime deep parity for real workspace tasks (npm/pnpm/yarn/ecosystem-aware runner).
5. Cross-version state migration and backward-compat schema evolution tests.
6. User-facing conflict resolution UI flow for ownership and settings merge conflicts.

## 2026-03-18 Incremental Update (Batch: settings-revision-ack)
- Added revision-aware remote settings merge acknowledgements in `src/core/settings-manager.mjs`.
- Added sync status persistence (`syncId`, `serverRevisionId`, `ack time`, counters) across restarts.
- Exposed shell bridge channel: `settings:sync-status`.
- Extended contracts:
  - `tests/core/core-contract.mjs` now checks ack entries and persisted server revision id.
  - `tests/shell/shell-contract.mjs` now checks `settings:sync-status` and merge ack fields.
- Validation passed:
  - `node tests/core/run-core-tests.mjs`
  - `node tests/shell/run-shell-tests.mjs`
  - `node tests/auth/run-all-checks.mjs`
  - `./launcher/open-reconstructed.command`

Note: this closes local revision/ack pipeline parity baseline. True server-authoritative reconciliation still requires real remote counterpart behavior evidence.
