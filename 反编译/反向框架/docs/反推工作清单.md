# Reverse Work Checklist (Execution Status)

Date: 2026-03-18
Scope: <CONTENTS_ROOT>/反编译/反向框架

1. Real protocol baseline: DONE
- Added shared protocol requester and signer hook.
- Added retry/timeout HTTP wrapper.

2. Provider interface coverage: DONE (auth scope)
- Bytedance/Marscode/SaaS now use the same protocol layer.
- login/refresh/check/userinfo/temp-token/logout branches are present.

3. IPC channel contract: DONE
- Added channel-spec validator and adapter.

4. Config parity baseline: DONE
- Layered default + env + runtime override model in config source.

5. Persistence/file layout: DONE (reconstructed scope)
- Added canonical path layout and JSON file store.
- Runtime bootstrap persists and clears user state.

6. Region/risk chain: DONE (policy hook)
- Added risk gate for blocked country/region checks.

7. Unified error model: DONE
- Added typed auth errors and provider code mapping.

8. Strict boundary contracts: DONE (runtime validators)
- Added validators for required response fields.

9. Test upgrade: DONE
- Added provider protocol / IPC / error contract tests.
- Added one-command full check runner.

10. Decompiled mapping audit: DONE (document baseline)
- Added mapping and gap file for per-module audit.

11. Documentation closure: DONE
- This checklist plus mapping doc provide completion snapshot.

12. Final acceptance gate: DONE
- `tests/auth/run-all-checks.mjs` runs full replay + contract suites.

13. Provider network context parity baseline: DONE
- Added provider-path route policy (`protocol-profile`) for token-required routing and provider-specific headers.
- Added request context (`request-context`) with requestId/traceId/device/version/platform/nonce/ts.
- Upgraded signer to v2 hash signature (`x-auth-sign-v: v2`).
- Added contract case: `tests/auth/contracts/network-context-contract.mjs`.

14. Provider endpoint payload template parity baseline: DONE
- Added centralized request templates (`request-template`) for ExchangeToken/GetUserInfo/CheckLogin payloads and protocol hints.
- Marscode/SaaS/Bytedance providers now build request payloads through shared templates (lower divergence and easier evidence alignment).
- Added contract case: `tests/auth/contracts/provider-payload-contract.mjs`.

## Note on “complete reverse engineering” definition
This completion means: reconstructed auth subsystem has full execution skeleton, deterministic offline validation, and explicit contracts for deep-dive replacement.
It does not claim binary-level equivalence of every hidden production endpoint without new live captures.

15. Provider route alignment + request fingerprint baseline: DONE
- Added route alignment rules (`route-alignment`) with provider/path required payload and header keys.
- Protocol requester now enforces alignment before network dispatch.
- Added request fingerprint recorder (`request-fingerprint-recorder`) with optional JSONL export via `TRAE_AUTH_FINGERPRINT_FILE`.
- Added contract cases: `tests/auth/contracts/request-alignment-contract.mjs`, `tests/auth/contracts/fingerprint-recorder-contract.mjs`.

16. Route alignment matrix automation + endpoint-diff batch-1: DONE
- Added auto generator: `replay/generate-route-alignment-matrix.mjs`.
- Added generated matrix doc: `docs/Provider路由对齐矩阵.md`.
- Added contract case: `tests/auth/contracts/alignment-matrix-doc-contract.mjs`.
- Added batch-1 route-specific payload builders in `request-template`:
  - `buildBytedanceTokenPayload`
  - `buildBytedanceRegionPayload`
  - `buildGenerateTempTokenPayload`
- Updated provider payload contract to enforce new dedicated builders.

17. Fingerprint evidence diff automation baseline: DONE
- Added evidence analyzer: `replay/analyze-fingerprint-evidence.mjs`.
- Added generated evidence report: `docs/Provider字段证据差异.md`.
- Added contract case: `tests/auth/contracts/evidence-diff-doc-contract.mjs`.
- Integrated generation chain in `tests/auth/run-all-checks.mjs`:
  - `generate-route-alignment-matrix`
  - `analyze-fingerprint-evidence`

18. Seeded fingerprint capture loop (baseline closed): DONE
- Added seeded capture generator: replay/seed-fingerprint-captures.mjs.
- Integrated auth check chain order:
  1) generate-route-alignment-matrix
  2) seed-fingerprint-captures
  3) analyze-fingerprint-evidence
- Evidence report now has capture rows (9/9) and status vocabulary is superseded by item 19 source-aware model.
- Baseline fingerprint file: docs/provider-fingerprints.jsonl (9 rows).

19. Source-aware evidence model (seed/live) closure: DONE
- Evidence analyzer upgraded to dual-source inputs:
  - Seed: `docs/provider-fingerprints.jsonl`
  - Live: `docs/provider-fingerprints.live.jsonl`
- Evidence report header now records both sources (`Seed Source`, `Live Source`).
- Status model upgraded to:
  - `no_capture`
  - `aligned_with_seed_only`
  - `aligned_with_live_capture`
  - `seed_capture_gap`
  - `live_capture_gap`
- Added live ingest script: `replay/register-live-fingerprint-captures.mjs`.
- Updated evidence doc contract to assert new header fields and status markers.
- Verified in full suite: `tests/auth/run-all-checks.mjs` passes with source-aware evidence flow.

20. Section 36 (branch/PR template governance): DONE
- Added `docs/第36节_分支与PR模板.md`.
- Defined branch naming, commit template, PR title/body template, and hard acceptance checklist.
- Purpose: keep reverse batches reviewable and auditable without changing runtime behavior.

21. Remote settings revision/ack baseline (local parity): DONE
- Upgraded settings merge to include sync envelope (`syncId`, `serverRevisionId`) and per-key ack entries.
- Added persisted sync status (`lastSyncId`, `lastServerRevisionId`, counters) in core state.
- Added shell bridge API: `settings:sync-status`.
- Extended contracts:
  - core contract validates ack + persisted sync revision.
  - shell contract validates `settings:sync-status` + merge ack shape.
- Full validation passed (core/shell/auth-all/launcher).

22. Live evidence coverage gate baseline: DONE
- Added `replay/live-evidence-coverage-gate.mjs` to compute live evidence coverage from evidence report.
- Supports strict mode via env:
  - `TRAE_AUTH_REQUIRE_LIVE_COVERAGE=1`
  - `TRAE_AUTH_LIVE_COVERAGE_MIN=<ratio>`
- Added contract: `tests/auth/contracts/live-evidence-coverage-contract.mjs` (relaxed pass + strict fail/pass paths).
- Integrated into pipelines:
  - `tests/auth/contracts/run-contract-tests.mjs`
  - `tests/auth/run-all-checks.mjs`
- Initial zero-live baseline is superseded by item 23 strict live bootstrap coverage baseline.

23. Strict live-coverage gate runnable baseline: DONE
- Added bootstrap live input file: `docs/provider-fingerprints.live.in.jsonl` (3 sample rows, marked `captureMode=bootstrap-sample`).
- Added strict runner: `tests/auth/run-live-coverage-strict-check.mjs`.
- Added transition note: `docs/在线证据引导说明.md`.
- Current measurable baseline after ingestion:
  - `aligned_with_live_capture = 3/9`
  - live coverage ratio = `0.3333`
- Strict gate verification passed:
  - `TRAE_AUTH_REQUIRE_LIVE_COVERAGE=1`
  - `TRAE_AUTH_LIVE_COVERAGE_MIN=0.3`

24. HAR-to-live evidence import baseline: DONE
- Added converter: `replay/import-live-fingerprints-from-har.mjs`.
- Supports filtering known auth routes and extracting provider/path/dataKeys/headerKeys into `provider-fingerprints.live.in.jsonl`.
- Added contract: `tests/auth/contracts/live-har-import-contract.mjs`.
- Integrated into auth contract suite via `tests/auth/contracts/run-contract-tests.mjs`.
- Verified in full checks (`auth-all` + strict live coverage check).

25. Live gap action-plan automation baseline: DONE
- Added generator: `replay/generate-live-gap-action-plan.mjs`.
- Output doc: `docs/在线差距行动计划.md` (Priority A/B/C + next commands).
- Added contract: `tests/auth/contracts/live-gap-plan-doc-contract.mjs`.
- Integrated into pipelines:
  - `tests/auth/contracts/run-contract-tests.mjs`
  - `tests/auth/run-all-checks.mjs`
- Verified in full checks and strict gate checks.

26. Full live coverage baseline (9/9) + strict default upgrade: DONE
- Promoted all previously `aligned_with_seed_only` routes to live sample coverage.
- Current evidence baseline: `aligned_with_live_capture = 9/9` (ratio = `1.0000`).
- Updated strict runner default in `tests/auth/run-live-coverage-strict-check.mjs`:
  - `TRAE_AUTH_LIVE_COVERAGE_MIN` default raised from `0.3` to `1.0`.
- Verified with default strict run and explicit `TRAE_AUTH_LIVE_COVERAGE_MIN=1`.

27. Real-vs-bootstrap live quality gate baseline: DONE
- Preserved `captureMode` in registered live fingerprints (`replay/register-live-fingerprint-captures.mjs`).
- Added quality gate: `replay/live-evidence-quality-gate.mjs`.
- Added contract: `tests/auth/contracts/live-evidence-quality-contract.mjs`.
- Added strict runner: `tests/auth/run-real-live-quality-strict-check.mjs`.
- Integrated quality gate into `tests/auth/run-all-checks.mjs` and contract suite.
- Current quality state (bootstrap baseline):
  - live coverage ratio = `1.0000` (9/9)
  - real live coverage ratio = `0.0000` (0/9)

28. Bootstrap-to-real replacement workflow baseline: DONE
- Added replacement tool: `replay/replace-bootstrap-with-real-captures.mjs`.
- Added contract: `tests/auth/contracts/replace-bootstrap-with-real-contract.mjs`.
- Integrated replacement contract into auth contract suite.
- Updated live bootstrap guide with staged real input path:
  - `docs/provider-fingerprints.live.real.in.jsonl`
- Verified in full auth-all checks.

29. Real capture promotion batch orchestrator baseline: DONE
- Added orchestrator: `replay/promote-real-capture-batch.mjs`.
- Supports mode:
  - `full` (default): import -> replace -> register -> evidence -> gates -> plan.
  - `import_replace_only`: sandbox-safe contract mode.
- Added contract: `tests/auth/contracts/real-capture-batch-contract.mjs`.
- Integrated contract into auth contract suite.
- Current measured real-live quality in baseline run:
  - real covered routes = `1/9`
  - bootstrap-only routes = `8/9`

30. Real HAR auto-discovery + one-click runbook closure: DONE
- Upgraded `replay/promote-real-capture-batch.mjs` with auto-discovery of newest `.har` under `docs/` (recursive) and env-controlled search root.
- Added one-click launcher: `launcher/promote-real-capture.command`.
- Added execution runbook: `docs/在线抓包运行手册.md`.
- Added contract: `tests/auth/contracts/real-har-auto-discovery-contract.mjs` and integrated into contract suite.
- Purpose: remove manual HAR path friction and speed real-capture replacement loop.

31. Phase-1 shell acceptance command closure: DONE
- Added one-click acceptance launcher: `launcher/run-shell-acceptance.command`.
- Added launcher contract: `tests/shell/shell-launcher-contract.mjs`.
- Integrated launcher contract into `tests/shell/run-shell-tests.mjs`.
- Added acceptance doc: `docs/阶段1验收.md`.
- Purpose: provide explicit executable gate for Phase-1 runtime shell.

32. Inferred high-fidelity route governance baseline: DONE
- Upgraded route matrix with explicit columns: `Rule Type`, `Evidence Level`, `Status`, `Evidence Note`.
- Added inferred register generator: `replay/generate-inferred-route-register.mjs` -> `docs/推断路由登记表.md`.
- Added inferred parity gate: `replay/inferred-route-parity-gate.mjs`.
- Integrated inferred register + gate into `tests/auth/run-all-checks.mjs`.
- Added contracts:
  - `tests/auth/contracts/inferred-route-register-contract.mjs`
  - `tests/auth/contracts/inferred-route-parity-gate-contract.mjs`
- Purpose: when live proof is missing, allow high-fidelity reconstruction only with explicit inferred marking.

33. Double-click visible UI fallback baseline (inferred): DONE
- `start-shell` now auto-opens local UI page after boot in normal mode (`TRAE_SHELL_OPEN_UI=1` by default).
- UI clearly marks itself as inferred high-fidelity shell view, not native renderer parity.
- Purpose: ensure double-click launch has visible interface feedback before native window layer is reconstructed.

34. Native app-bundle layout restoration baseline: DONE
- Re-laid out `Contents` to native macOS structure roots: `MacOS`, `Frameworks`, `Resources`.
- Restored bundle metadata to `Contents` root:
  - `Info.plist`
  - `PkgInfo`
  - `_CodeSignature/CodeResources`
- Added native launch diagnostics:
  - `launcher/check-native-layout.command`
  - `launcher/open-native-contents.command`
- Added Finder-openable app wrapper: `<CONTENTS_ROOT>/../Trae CN.app` (Contents symlink to restored bundle).
- Added app-bundle launcher: `launcher/open-native-appbundle.command`.
- Purpose: move from simulated shell UI toward original native window startup path.
