# Near-Original Full Rebuild Plan

Date: 2026-03-18
Target: Reconstructed project reaches near-original runnable parity (double-click startup, major flows usable, behavior close to upstream).

## Success Definition
1. Double-click launcher starts app reliably on target machine.
2. Main workflows (login, project/session, settings, extension hooks, cloud sync baseline) are runnable.
3. Auth/network/config/IPC/storage behaviors are contract-checked and replay-backed.
4. Regression gate is one-command and deterministic.

## Phase Breakdown

### Phase 0: Baseline Freeze (Done)
- Auth reconstruction skeleton + replay suite + contract tests.
- One-command gate: `tests/auth/run-all-checks.mjs`.

### Phase 1: Runtime Shell Rebuild (In Progress)
- Rebuild launcher + app bootstrap chain.
- Rebuild main process lifecycle (window/menu/tray/log/crash handling).
- Rebuild renderer bootstrap and bridge contract.
- Output artifact: double-click script starts reconstructed shell.

### Phase 2: Core Domain Modules
- Workspace/session/project open pipeline.
- Settings and runtime preference chain.
- Local state indexing and cache lifecycle.
- Output artifact: open project and persist/reload state.

### Phase 3: Extension + Toolchain Layer
- Extension host lifecycle and minimal API bridge.
- Command routing and contribution point loading.
- Sandboxed task execution interfaces.
- Output artifact: key extension path works with deterministic fallback.

### Phase 4: Cloud + Sync + Risk Controls
- Provider deep protocol parity (dynamic params/signature details from evidence).
- Sync + account-scoped settings merge.
- Region/risk policy enforcement with observable errors.
- Output artifact: network-dependent flows close to original behavior.

### Phase 5: Distribution + Packaging
- Bundle scripts, resource layout, startup wrappers.
- Double-click launcher for reconstructed runtime.
- Startup diagnostics and self-check output.
- Output artifact: end-user style runnable package.

### Phase 6: Parity Burn-down
- Module-by-module diff audit against decompiled map.
- Close behavior gaps with targeted replay and contract cases.
- Output artifact: near-original parity report with residual gaps list.

## Hard Gates
1. Every phase must have executable acceptance command.
2. New module work must include mapping note + test evidence.
3. No silent fallback for auth/risk/network errors.
4. No destructive overwrite of decompiled source artifacts.

## Current Critical Path
1. Build shell bootstrap scaffold (Phase 1).
2. Wire launcher script for double-click run.
3. Add shell acceptance script and include in global gate.

## Known Remaining Deep Gaps
- Production anti-abuse signature details require additional live evidence.
- Native binary dependencies and packaging behavior may differ across machines.
